import test from '@/uni_modules/uview-ui/libs/function/test.js'

export default {
	
	//用户注册
	register:{
		// 用户名
		accountName: [{
				required: true,
				message: '请输入用户名',
			},
			{
				min: 6,
				message: '用户名最少6个字符',
			},
		],
		
		
		// 密码
		passWord: [{
				required: true,
				message: '请输入密码',
			},
			{
				pattern: /^(?![0-9]+$)(?![a-zA-Z]+$)[0-9A-Za-z]+\S{5,12}$/,
				message: '密码需同时含有字母和数字，长度在6-12之间',
			}
		],
		
		// 确认密码
		confirmPassWord: [{
				required: true,
				message: '请输入确认密码',
			},
			{
				pattern: /^(?![0-9]+$)(?![a-zA-Z]+$)[0-9A-Za-z]+\S{5,12}$/,
				message: '确认密码需同时含有字母和数字，长度在6-12之间',
			},
			
			{
				validator: (rule, value, callback) => {
					let passWord = value.split(",")[0];
					let confirmPassWord = value.split(",")[1];
					return passWord === confirmPassWord;
				},
				message: '密码和确认密码需相同',
			}
		
		],
		
		// 全名
		fullName: [{
				required: true,
				message: '请输入全名',
			},
		],
		
		// 手机号
		phoneNumber: [{
				required: true,
				message: '请输入手机号',
			},
			{
				validator: (rule, value, callback) => {
					return test.mobile(value);
				},
				message: '手机号不符合规则',
			}
		],
		
		// 短信验证码
		verificationCode: [{
				required: true,
				len: 6,
				message: '请输入6位验证码',
			},
		]
	},

	
	accountLogin:{
		//用户名/手机号
		loginName: [{
				required: true,
				message: '请输入用户名/手机号',
			},
			
		],
		
		// 密码
		passWord:[{
			required: true,
			message: '请输入密码',
		}]
	},
	
	forgotPwd:{
		// 手机号
		phoneNumber: [{
				required: true,
				message: '请输入手机号',
			},
			{
				validator: (rule, value, callback) => {
					return test.mobile(value);
				},
				message: '手机号不符合规则',
			}
		],
		
		// 短信验证码
		verificationCode: [{
				required: true,
				len: 6,
				message: '请输入6位验证码',
			},
		],
		
		// 密码
		passWord: [{
				required: true,
				message: '请输入密码',
			},
			{
				pattern: /^(?![0-9]+$)(?![a-zA-Z]+$)[0-9A-Za-z]+\S{5,12}$/,
				message: '密码需同时含有字母和数字，长度在6-12之间',
			}
		],
		
		// 确认密码
		confirmPassWord: [{
				required: true,
				message: '请输入确认密码',
			},
			{
				pattern: /^(?![0-9]+$)(?![a-zA-Z]+$)[0-9A-Za-z]+\S{5,12}$/,
				message: '确认密码需同时含有字母和数字，长度在6-12之间',
			},
			
			{
				validator: (rule, value, callback) => {
					let passWord = value.split(",")[0];
					let confirmPassWord = value.split(",")[1];
					return passWord === confirmPassWord;
				},
				message: '密码和确认密码需相同',
			}
		],
	},
	
	
	changePwd:{
		//旧密码
		oldPassWord: [{
				required: true,
				message: '请输入旧密码',
			},
		],
		
		// 密码
		passWord: [{
				required: true,
				message: '请输入密码',
			},
			{
				pattern: /^(?![0-9]+$)(?![a-zA-Z]+$)[0-9A-Za-z]+\S{5,12}$/,
				message: '密码需同时含有字母和数字，长度在6-12之间',
			}
		],
		
		// 密码
		passWord: [{
				required: true,
				message: '请输入密码',
			},
			{
				pattern: /^(?![0-9]+$)(?![a-zA-Z]+$)[0-9A-Za-z]+\S{5,12}$/,
				message: '密码需同时含有字母和数字，长度在6-12之间',
			}
		],
		
		// 确认密码
		confirmPassWord: [{
				required: true,
				message: '请输入确认密码',
			},
			{
				pattern: /^(?![0-9]+$)(?![a-zA-Z]+$)[0-9A-Za-z]+\S{5,12}$/,
				message: '确认密码需同时含有字母和数字，长度在6-12之间',
			},
			
			{
				validator: (rule, value, callback) => {
					let passWord = value.split(",")[0];
					let confirmPassWord = value.split(",")[1];
					return passWord === confirmPassWord;
				},
				message: '密码和确认密码需相同',
			}
		
		],
		
	}
	
	
	
	
}
