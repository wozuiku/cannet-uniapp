import { mapMutations, mapActions, mapState, mapGetters } from 'vuex';

export default {
	
	data(){
		return {
			userId:'',
		}
	},
	
	
	onLoad(){
		let userInfo = uni.getStorageSync('userInfo')
		this.userId = userInfo.id
		console.log('user onLoad userInfo', userInfo)
		console.log('user onLoad userId', this.userId)
	},
	
	onShow(){
		let userInfo = uni.getStorageSync('userInfo')
		this.userId = userInfo.id
		console.log('user onShow userInfo', userInfo)
		console.log('user onShow userId', this.userId)
	},
	
	computed: {
		...mapGetters(['isLogin', 'userInfo', 'authType']),
	},
	
	methods: {
		jump(){
			this.$Router.push({path: '/pages/user/room/index/index', query: {userId: this.userId}});
		},
	},
}